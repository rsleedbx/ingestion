type: DATABRICKS_DELTALAKE

url: "${DBX_SPARK_URL}"
username: token
password: "${DBX_ACCESS_TOKEN}"
host: "${DBX_HOSTNAME}"
port: 443
max-connections: 10

stage:
  type: DATABRICKS_DBFS
  root-dir: "${DBX_DBFS_ROOT}"
  use-credentials: true
  file-format: CSV

