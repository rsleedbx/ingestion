#!/usr/bin/env bash

# of osx
brew install openjdk@8  # can't install on ARM64

brew install openjdk@17  # can't install on ARM64

sudo apt-get install -y openjdk-17-jre-headless  # version 17.0.2+8-1
